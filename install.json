{
  "resources": {
    "head": [
      {
        "type": "script",
        "src": "./head.js"
      }
    ]
  },
  "preview": {
    "handlers": [
      {
        "options": ["*"],
        "execute": "INSTALL_SCOPE.setOptions(INSTALL_OPTIONS)"
      }
    ]
  },
  "options": {
    "properties": {
      "css": {
        "title": "CSS Code",
        "type": "string",
        "format": "code"
      }
    }
  }
}
